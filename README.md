# NumPy Tutorial - Google Colab

This repository contains a **complete collection of NumPy exercises and examples** recorded in Google Colab. It covers **fundamental operations, array manipulations, and mathematical functions** for beginners and intermediate learners.

---

## 📚 Topics Covered

1. **Introduction to NumPy**
   - What is NumPy
   - Why NumPy arrays over Python lists

2. **Array Creation**
   - `array()`, `arange()`, `zeros()`, `ones()`, `linspace()`
   - 1D, 2D, and 3D arrays
   - Reshaping arrays
   - Adding new axes

3. **Array Attributes**
   - `ndim`, `shape`, `size`, `dtype`

4. **Array Operations**
   - Arithmetic: addition, subtraction, multiplication, division
   - Aggregate functions: `sum`, `min`, `max`, `mean`, `std`, `prod`

5. **Random Numbers**
   - `rand()`, `randn()`, `randint()`

6. **Indexing and Slicing**
   - 1D and 2D indexing
   - Slicing arrays
   - Row-wise and column-wise selection

7. **Array Manipulations**
   - Concatenation: `concatenate`, `hstack`, `vstack`
   - Splitting: `hsplit`, `vsplit`
   - Flattening: `flatten()`, `ravel()`
   - Transpose: `.T`
   - Reverse arrays: slicing `[::-1]`, `np.flip()`
   - View vs Copy (shallow vs deep copy)

---

## 🔹 How to Use

1. Open the Google Colab notebooks provided in this repository.
2. Run each cell to understand **NumPy concepts with examples**.
3. Modify arrays and experiment with **different operations** to practice.

---

## 🔹 Why This Repository

This collection is ideal for:
- **Beginners in Python & Data Science**
- **Students preparing for exams**
- **Anyone learning NumPy for Machine Learning or AI**

---

## 🛠 Tools Used

- Python 3.x  
- NumPy Library  
- Google Colab  

---


